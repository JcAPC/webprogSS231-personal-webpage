.container {
    display: grid;
    grid-template-rows: auto 1fr auto;
    height: 100vh;
}

/* Header and Footer */
header, footer {
    background-color: #f0f0f0;
    padding: 20px;
    text-align: center;
}

/* Main Section */
main {
    display: flex;
    flex-direction: row;
}

/* Main Content */
.main-content {
    flex: 2;
    background-color: #ffffff;
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
}

/* Child elements inside .main-content */
.flex-container > div {
    background-color: #f1f1f1;
    width: 100px;
    margin: 10px;
    text-align: center;
    line-height: 75px;
    font-size: 30px;
}

/* Sidebar */
.sidebar {
    flex: 1;
    background-color: #f5f5f5;
    padding: 20px;
}
