/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    padding: 20px;
    line-height: 1.6;
}

/* Main Container */
.container {
    display: flex;
    flex-wrap: wrap;
    gap: 20px; /* Adds spacing between sections */
    border: 2px solid #000;
    padding: 20px;
    box-sizing: border-box; /* Ensures proper width calculations */
}

/* Header and Footer */
header, footer {
    width: 100%;
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px 0;
    box-sizing: border-box;
}

/* Main Section */
main {
    display: flex;
    flex-wrap: wrap;
    gap: 20px; /* Adds spacing between main content and sidebar */
}

/* Main Content */
.main-content {
    flex: 2; /* Takes up more space than the sidebar */
    background-color: #f0f0f0;
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px; /* Adds spacing between child elements */
    border: 2px solid #ccc;
    box-sizing: border-box;
}

.main-content div {
    flex: 1 1 200px; /* Ensures child items wrap and maintain at least 200px width */
    padding: 10px;
    background-color: #e0e0e0;
    border: 1px solid #aaa;
    text-align: center;
}

/* Sidebar */
.sidebar {
    flex: 1; /* Takes up less space than the main content */
    min-width: 200px; /* Prevents sidebar from shrinking too much */
    background-color: #ddd;
    padding: 20px;
    border: 2px solid #bbb;
    box-sizing: border-box;
}

/* Images Section */
img {
    margin: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}
