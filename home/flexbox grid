/* Main Container */
.container {
    display: grid;
    grid-template-rows: auto 1fr auto;
    height: 100vh;
}

/* Header and Footer */
header, footer {
    background-color: #f0f0f0;
    padding: 20px;
    text-align: center;
}

/* Main Section */
main {
    display: flex;
    flex-direction: row;
    gap: 20px; /* Added for spacing */
}

/* Main Content */
.main-content {
    flex: 2;
    background-color: #ffffff;
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px; /* Added for spacing between child elements */
}

/* Child elements inside .main-content */
.main-content > div {
    background-color: #f1f1f1;
    width: calc(33.33% - 20px); /* Adjusted for wrapping and spacing */
    min-width: 150px; /* Ensures responsiveness */
    padding: 20px;
    text-align: center;
    font-size: 18px;
    border: 1px solid #ccc;
    box-sizing: border-box;
}

/* Sidebar */
.sidebar {
    flex: 1;
    min-width: 200px; /* Ensures sidebar doesn't shrink too much */
    background-color: #f5f5f5;
    padding: 20px;
}

